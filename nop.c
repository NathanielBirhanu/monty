#include "monty.h"
/**
 * nop - Does nothing (No operation)
 * @stack: Pointer to the stack
 * @line_number: Line number where the nop operation is performed
 */
void nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
